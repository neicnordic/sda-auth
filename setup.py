from setuptools import setup, find_packages

setup(name='sda_auth',
      author='NBIS',
      version='0.1.0',
      description='SDA authentication backend',
      packages=find_packages(),
      include_package_data=False,
      )
